apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: blog
  labels:
    name: blog

spec:
  template:
    metadata:
      labels:
        app: blog
    spec:
      containers:
      - name: blog
        image: bencord0/blog
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: blog-env
        livenessProbe:
          httpGet:
            path: /
            port: 8000
